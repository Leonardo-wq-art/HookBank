
const Button = (styles) => {
  return (
 
    <button type='button' className={` flex items-center justify-center text-center 
     bg-blue-800 font-medium v4 border border-blue-800
     text-white outline-none  mt-5 rounded-xl p-3`}>
    <p> Get started</p>

    </button>

  )
}

export default Button